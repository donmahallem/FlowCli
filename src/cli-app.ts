

export class CliApp {
    constructor() {
        /*
        nconf.argv({
            'u': {
                alias: 'user',
                describe: 'Example description for usage generation',
                demand: true,
                parseValues: true,
                transform: function (obj) {
                    if (obj.key === 'foo') {
                        obj.value = 'baz';
                    }
                    return obj;
                }
            },
            'p': {
                alias: 'password',
                describe: 'Example description for usage generation',
                demand: true,
                parseValues: true,
                transform: function (obj) {
                    if (obj.key === 'foo') {
                        obj.value = 'baz';
                    }
                    return obj;
                }
            }
        })*/
    }
    public start(): void {

    }
}